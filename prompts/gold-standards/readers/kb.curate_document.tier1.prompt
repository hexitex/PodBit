Distributed consensus algorithms Raft and Paxos enable multiple servers to agree on a sequence of values despite network partitions and failures. Raft separates the problem into leader election using randomized timeouts, log replication from the leader to followers, and safety guarantees ensuring at most one leader per term. A cluster of N servers tolerates up to (N-1)/2 failures, with entries considered committed once a majority has persisted them.