/**
 * Graph handlers — barrel re-export.
 *
 * Delegates to graph/ subdirectory modules for node CRUD operations:
 * propose, query, read (get/lineage), modify (remove/edit), and validate.
 */
export * from './graph/index.js';
